# 复制为 .env 或 .env.production 后按需修改（勿提交含密钥的 .env 到仓库）
#
# 管理台与 API 不同源时填写 API「根」地址：无尾部斜杠、不要带 /api
# VITE_API_BASE_URL=https://vpn-api.example.com
#
# 重要：若管理台页面是 https:// 开头，这里也必须用 https://，不能用 http://，
# 否则浏览器会拦截「混合内容」，表现为构建后无法登录、接口全失败。
#
# 若页面与 API 同域名（Nginx 把 /api 反代到 vpn-api），请留空本变量，不要写地址。
# VITE_API_BASE_URL=
